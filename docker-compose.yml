services:
  # 🧠 Local MongoDB
  mongodb:
    image: mongo:latest
    container_name: dev-mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db

  # ⚡ Local Redis
  redis:
    image: redis:7-alpine
    container_name: dev-redis
    ports:
      - "6379:6379"

volumes:
  mongodb_data:
